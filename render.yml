services:
  - name:   Car-Price-Prediction
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        value: "8080"  # Use a port of your choice

build:
  name: Car-Price-Prediction
